<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_background"
    android:elevation="8dp">

    <!-- Icon Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingTop="24dp"
        android:paddingBottom="8dp">

        <TextView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:text="🚪"
            android:textSize="48sp"
            android:gravity="center"
            android:background="@drawable/circle_background"
            android:backgroundTint="@color/premium_warning_light"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Logout"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/premium_text_primary"
            android:layout_marginBottom="16dp"/>

        <View
            android:layout_width="60dp"
            android:layout_height="3dp"
            android:background="@color/premium_primary"
            android:layout_marginBottom="16dp"/>

    </LinearLayout>

    <!-- Message -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Are you sure you want to logout? You'll need to login again to access your account."
        android:textSize="16sp"
        android:textColor="@color/premium_text_secondary"
        android:lineSpacingExtra="4dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="24dp"
        android:gravity="center"/>

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="20dp"
        android:divider="?android:attr/dividerHorizontal"
        android:showDividers="middle">

        <Button
            android:id="@android:id/button2"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Cancel"
            android:textSize="14sp"
            android:textColor="@color/premium_text_secondary"
            android:background="@drawable/dialog_button_border"
            android:layout_marginEnd="8dp"
            app:cornerRadius="24dp"/>

        <Button
            android:id="@android:id/button1"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Logout"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:background="@drawable/dialog_button_logout"
            android:layout_marginStart="8dp"
            app:cornerRadius="24dp"/>

    </LinearLayout>

</LinearLayout>